Low-level network servers (TCP|UDP) in Go, managed on AWS EC2 with Terraform for provisioning, deployment and termination, syncing code via GitHub, kickstarting local instance using Docker and a custom .sh script.

Server submissions for protohackers.com
